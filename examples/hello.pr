// Protorunの簡単なサンプルプログラム

// 整数を2倍にする関数
fn double(x: Int): Int = x * 2;

// 挨拶を返す関数
fn greet(name: String): String = "Hello, " + name + "!";

// メイン関数
fn main(): Unit = {
    let x = 42;
    let y = double(x);
    let message = greet("Protorun");
    
    // 結果の表示（実際のランタイムが実装されるとここで出力される）
    message
};
