VERIFICATION: Function Syntax Updated

✓ grep -n "fn(x:" AGENTS.md
  Result: (no output = all removed)

✓ Syntax Rules table updated:
  OLD: | Function body | `fn(x: Int) -> Int = x + 1` | `fn(x: Int) -> Int => x + 1` |
  NEW: | Function body | `(x: Int) -> Int = x + 1` | `(x: Int) -> Int => x + 1` |

✓ Return type row also updated:
  OLD: | Return type | `fn(x: Int) -> Int` | `fn(x: Int): Int` |
  NEW: | Return type | `(x: Int) -> Int` | `(x: Int): Int` |

✓ No remaining "fn(" syntax in AGENTS.md
