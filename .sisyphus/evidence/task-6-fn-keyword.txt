## Task 6 — fn keyword removal evidence

Date: 2026-03-20

### Action
Deleted the `fn` keyword row from the keyword table in docs/language-spec/02-lexical-structure.md.

### Reason
`fn` was removed from the Keyword production in 12-grammar.md (T2).
The grammar now uses lambda syntax without `fn` keyword:
  Keyword ::= "let" | "mut" | "impl" | "return" | "if" | "elif" | "else"
            | "match" | "for" | "in" | "while" | "loop" | "break" | "continue" | "then"
            | "trait" | "type" | "alias" | "module" | "import" | "as"
            | "with" | "effect" | "where" | "True" | "False"

Function expressions use `(params) -> ReturnType = body` syntax without `fn`.

### Verification
grep -n "| fn |" docs/language-spec/02-lexical-structure.md
→ No matches (confirmed)

### bind keyword status
`bind` row was updated to mark as "将来検討" (future consideration).
Reason: `BindExpr` is not defined in the current EBNF grammar.
The row now reads: "現在のEBNFには`BindExpr`の定義なし。将来フェーズで導入予定。"
